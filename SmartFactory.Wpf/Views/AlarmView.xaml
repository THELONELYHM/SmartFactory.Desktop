<UserControl x:Class="SmartFactory.Wpf.Views.AlarmView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="报警中心" Style="{StaticResource PageTitle}"/><Grid Grid.Row="1" Margin="0,20,0,16"><Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="170"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding SearchText,UpdateSourceTrigger=PropertyChanged}" ToolTip="搜索设备或报警内容"/><ComboBox Grid.Column="1" ItemsSource="{Binding LevelOptions}" SelectedItem="{Binding SelectedLevel}" Margin="10,0"/><StackPanel Grid.Column="3" Orientation="Horizontal"><Button Content="刷新" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/><Button Content="确认报警" Command="{Binding AcknowledgeCommand}" Style="{StaticResource PrimaryButton}"/></StackPanel></Grid><Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="2"><DataGrid ItemsSource="{Binding Alarms}" SelectedItem="{Binding SelectedAlarm}"><DataGrid.Columns><DataGridTextColumn Header="发生时间" Binding="{Binding OccurredAt,StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="160"/><DataGridTextColumn Header="设备" Binding="{Binding DeviceCode}" Width="100"/><DataGridTemplateColumn Header="级别" Width="90"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Background="{Binding Level,Converter={StaticResource AlarmLevelBrush}}" CornerRadius="10" Padding="8,3" HorizontalAlignment="Left"><TextBlock Text="{Binding Level}" Foreground="White" FontSize="11"/></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="报警内容" Binding="{Binding Message}" Width="*"/><DataGridCheckBoxColumn Header="已确认" Binding="{Binding IsAcknowledged}" Width="75"/><DataGridTextColumn Header="确认人" Binding="{Binding AcknowledgedBy}" Width="110"/><DataGridTextColumn Header="确认时间" Binding="{Binding AcknowledgedAt,StringFormat={}{0:MM-dd HH:mm}}" Width="110"/></DataGrid.Columns></DataGrid></Border><Grid Grid.Row="3" Margin="0,14,0,0"><TextBlock Text="{Binding TotalCount,StringFormat=共 {0} 条报警}" Foreground="{StaticResource TextSecondaryBrush}"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="上一页" Command="{Binding PreviousCommand}" Style="{StaticResource SecondaryButton}"/><TextBlock Text="{Binding PageIndex}" Margin="14,0" VerticalAlignment="Center"/><TextBlock Text="/" VerticalAlignment="Center"/><TextBlock Text="{Binding TotalPages}" Margin="14,0" VerticalAlignment="Center"/><Button Content="下一页" Command="{Binding NextCommand}" Style="{StaticResource SecondaryButton}"/></StackPanel></Grid></Grid></UserControl>
<!-- 报警中心页面：级别过滤、分页列表和权限控制的确认按钮。 -->
<!--
  报警表格绑定分页后的 Alarms 集合；级别使用 Converter 映射为视觉色，
  确认按钮绑定 AcknowledgeCommand，其 CanExecute 会根据 RBAC 自动禁用。
-->
